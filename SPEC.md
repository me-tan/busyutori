# 部首アタック — 開発仕様書

最終更新: 2026-09-03
提出期限: **2026年9月13日（日）** — 学力向上アプリコンテスト 第7回

---

## 0. このドキュメントについて

VSCode 上で開発を継続するための引き継ぎ資料。
現在の実装状態、設計判断の理由、未解決の課題を記載する。

**いま最優先で解決したい課題は「6. 現在の課題」に記載。**

---

## 1. 作品概要

### コンセプト

提示された部首を含む漢字を、制限時間内に手書きで書き出すゲーム。
一度使った漢字は二度と使えないため、続けるほど自分の知識の底が試される。

### 中心となる主張（応募資料用）

> 既存の漢字アプリは、書けた字を採点する。
> 本作は、**書けなかった字を教える。**

ゲーム終了時に、その部首でまだ使われていなかった漢字を提示する。
**負けた瞬間に学びが最大化される**ことが設計の核。

### 対象

小学生〜高校生。学年配当に応じて出題範囲を切り替える。

### コンテストの評価基準（重要）

主催者は「**作品のアイデアを最も重視する**」と明記。加えて、
- 必要な機能が備わっているか
- 使い勝手が良いか
- **見た目が洗練されているか**

第5回最優秀賞「マスmatics」（高校生の作品）は、作者自身が
「見てもらいたいのは**使い心地の良さ**」「画面のなめらかな動き」「指でなぞる操作性」
と述べている。**機能の多さではなく、完成度で評価される。**

---

## 2. 技術構成

| 項目 | 内容 |
|---|---|
| 形態 | 静的Webサイト（HTML/CSS/JS のみ） |
| サーバー | **不要** |
| データベース | **不要** |
| 外部通信 | **なし**（完全にオフラインで動作） |
| 手書き認識 | KanjiCanvas（クライアントサイドJS） |
| 想定環境 | PC のブラウザ（Chrome / Edge / Safari） |

### ファイル構成

```
プロジェクトフォルダ/
├── game.html          メインの実装（HTML/CSS/JS がすべて入っている）
├── radicals.json      部首・漢字データ
├── kanji-canvas.js    KanjiCanvas 本体（外部ライブラリ）
├── ref-patterns.js    KanjiCanvas の参照パターンデータ（外部ライブラリ）
├── LICENSE            本プロジェクトのライセンス（MIT）
└── third_party-licenses/
    └── kanjicanvas-LICENSE.TXT   KanjiCanvas のライセンス（同梱必須）
```

> **注記（2026-09-04）:** 当初 `license/kanjicanvas-LICENSE.TXT` という配置だったが、
> ルートに `LICENSE`（プロジェクト本体のMIT）を追加した際、大文字小文字を区別しない
> ファイルシステム（macOS APFSのデフォルト設定など）では `LICENSE` と `license/` が
> 同一パスとして衝突することが判明したため、`third_party-licenses/` に改名した。

### 起動方法

`radicals.json` を fetch するため、`file://` では動作しない。

```bash
python3 -m http.server 8000
# http://localhost:8000/game.html
```

### 外部ライブラリのライセンス

**KanjiCanvas** — https://github.com/asdfjkl/kanjicanvas

```
Copyright (c) 2019-2024 Dominik Klein
Copyright (c) 2020 Seth Clydesdale
licensed under MIT (cf. third_party-licenses/kanjicanvas-LICENSE.TXT).
```

MIT ライセンスのため商用・改変・再配布が可能。
**配布物に third_party-licenses/kanjicanvas-LICENSE.TXT を同梱すること。**
応募資料にも「手書き認識に KanjiCanvas（MIT License）を使用」と明記する。

### API

```javascript
KanjiCanvas.init('can');          // canvas の id を渡して初期化
KanjiCanvas.recognize('can');     // 認識結果を「空白区切りの文字列」で返す
KanjiCanvas.deleteLast('can');    // 一画取り消し
KanjiCanvas.erase('can');         // 全消去
```

`recognize()` は現状 **10候補程度**を返す。筆順・画数フリー。

---

## 3. データ仕様（radicals.json）

### 生成元

- **CHISE IDS**（cjkvi-ids）— 漢字の字形分解データ
- **KANJIDIC2 派生データ** — 学年・読み

### 構造

```json
{
  "meta": {
    "radicalCount": 61,
    "kanjiCount": 1602,
    "grades": { "1":"小1", ..., "8":"中学以降" }
  },
  "radicals": {
    "氵": {
      "name": "さんずい",
      "meaning": "水に関係する",
      "kanji": {
        "2": ["池","汽","活","海"],
        "3": ["氷","決","油", ...]
      },
      "count": { "all": 112, "elem": 43, "low": 20 }
    }
  },
  "kanji": {
    "海": { "grade": 2, "on": ["かい"], "kun": ["うみ"], "readings": ["うみ","かい"] }
  }
}
```

### 設計判断: 「部首」の定義

**伝統的な部首分類（康熙部首）ではなく、字形に含まれる部品として判定している。**

理由:
1. CHISE IDS からそのまま導出でき、判定が機械的に確定する
2. プレイヤーの直感（「さんずいの漢字」）と一致する
3. 伝統的部首は直感とずれる場合がある

この判定方針に伴い、以下の調整を行った。

- **呼称から位置を示す語を除いた。**
  「つちへん」→「つち」、「きへん」→「き」。
  土は左だけでなく上下にも来る（王・主・寺）ため、「へん」と呼ぶと嘘になる。
  一方、氵・艹のように位置が固定されている部首は「さんずい」「くさかんむり」のまま。

- **意味がばらつく部首の説明文を「〜の形が入っている」に変えた。**
  口を「口や声に関係する」とすると京・高・船が説明できない。
  「口の形が入っている」なら全て正しい。

- **分解は1階層のみ。**
  再帰的に分解すると「語」が口にも該当してしまい直感とずれる。

### 設計判断: 異体字の統合と分離

**統合したもの**（同じ形でコードが違うだけ）

```
⺮→竹, 糸→糹, ⺡→氵, ⺘→扌, ⺅→亻, ⺾/艸→艹,
⻌/辵→辶, ⺖→忄, ⺬→礻, ⻂→衤, ⻖/⻏→阝,
⺩/玉→王, ⺨→犭, ⺣→灬, ⺼→月, 刀/⺈/⺉→刂,
攴/⺙→攵, ⺧/牜→牛
```

**あえて分離したもの**（形が違い、直感的に別物）

| 分離 | 理由 |
|---|---|
| 亻 / 人 | 亻92字は全てにんべん。人16字は傘・企・全・舎など。統合すると大・火・金がにんべん扱いになる |
| 扌 / 手 | 扌86字は全ててへん。手6字は挙・拳・掌・摩・撃 |
| 火 / 灬 | 灬18字（点・照・熱・黒・馬・魚・鳥）は火に見えない |
| 衤 / 衣 | 衤10字（初・補・被）、衣7字（製・裁・装） |
| 礻 / 示 | 礻12字（社・神・福）、示8字（祭・票・禁） |
| 犭 / 犬 | 犭13字（犯・独・猫）、犬10字（器・状・献） |

**除外したもの**

- 肉（腐1字のみ）
- 牜（0字。牛に統一）

### データの再生成

生成スクリプトは `build_radicals.py`（別途保管）。
`RADICALS` 辞書に部首と名称・説明を列挙し、`VARIANT` で異体字を統合している。
出題対象の部首は**手作業でキュレーションしている**（機械的な集計には「一」「丿」等の
直感的でない部品が混ざるため）。

---

## 4. ゲームルール

### 共通

1. 提示された部首を含む漢字を、canvas に手書きする
2. 「これでいく」を押すと KanjiCanvas が認識
3. 認識候補のうち**条件を満たすものだけ**を選択肢として表示
4. プレイヤーがタップして確定 → 正解
5. 一度使った漢字は再使用不可

### 正解の条件

- 出題された部首を含む
- 選択した難易度の範囲内の学年である
- まだ使われていない

### 不正解時のメッセージ

原因を明示する（黙って弾かない）。

| 状況 | 表示 |
|---|---|
| 既に使った | 「『海』はもう書きました」 |
| 部首は合うが範囲外の学年 | 「『湿』は中学で習う漢字です」 |
| 該当なし | 「その部首を含む漢字が見つかりません」 |

### 難易度

| 難易度 | 範囲 | 出題可能な部首 |
|---|---|---|
| 初級 | 小1〜小3 | 15種 |
| 中級 | 小1〜小6 | 25種 |
| 上級 | 常用漢字すべて | 29種 |

低学年ほど該当漢字が少ないため、**学年ごとではなく累積範囲**とする。

### モード1: タイムアタック

- 制限時間 **30秒**（`TIME_LIMIT` 定数）
- 正解すると次の部首へ。時間はリセット
- 時間切れで終了。連続正解数がスコア
- 同じ部首が連続しないよう、直近2回に出た部首を避ける
- 未使用漢字が5字未満の部首は出題対象から外れる
- 使用済み漢字は画面下に一覧表示。**現在の部首に該当する字だけ濃く表示**

### モード2: コンプリート

- 部首を1つ選び、その部首の漢字を**すべて**書き出す
- **制限時間なし**
- 対象の字数だけ点線の箱が並び、正解すると埋まる
- 全部埋めると「コンプリート」＋所要時間を表示
- 達成した部首は一覧で緑色に
- 部首一覧は**字数の少ない順**（挑みやすい順）
- 「ここでやめる」で中断可能

### 結果画面（本作の中心機能）

最後に出題された部首について、**使われなかった漢字を提示する。**

```
氵を含む漢字は、まだ 37 字ありました

[海 うみ 小2] [池 いけ 小2] [泳 およぐ 小3] ...

あなたが書けたのは 汽 活 の 2 字
```

- 学年順に並べる
- 読みは**訓読みを優先**（「うみ」の方が「かい」より思い出しやすい）
- 上限 24字（`MAX_REVEAL`）、それ以上は「ほか◯字」

---

## 5. コードの構造（game.html）

単一ファイル構成。上から順に CSS → HTML → JS。

### 画面

`show(id)` で切り替える。5画面。

```
modeScreen   モード選択（タイムアタック / コンプリート）
levelScreen  難易度選択
radScreen    部首選択（コンプリートのみ）
playScreen   プレイ画面（両モード共用）
overScreen   結果画面（両モード共用）
```

### 状態

```javascript
state = {
  mode,        // 'time' | 'complete'
  level,       // 'low' | 'elem' | 'all'
  used,        // Set<漢字>  使用済み
  streak,      // 連続正解数
  radical,     // 現在の部首
  recent,      // 直近に出した部首（連続回避）
  timeLeft, timerId,
  locked,      // 正解演出中の入力停止フラグ
  target,      // コンプリート対象の漢字配列
  startedAt    // コンプリート開始時刻
}
cleared        // Set<'難易度:部首'>  コンプリート達成記録
```

### 主要関数

| 関数 | 役割 |
|---|---|
| `boot()` | radicals.json 読み込み、KanjiCanvas 初期化 |
| `poolOf(rad, grades)` | 部首と学年配列から対象漢字の配列を返す |
| `pickRadical()` | タイムアタックの出題部首を選ぶ |
| `judge()` | 認識 → 候補の絞り込み → 選択肢の表示 |
| `accept(k)` | 正解確定。モードごとの後処理 |
| `renderBoxes()` | コンプリートの箱を描画 |
| `renderReveal()` | 結果画面の未使用漢字を描画 |
| `gameOver(reason)` | 終了処理。reason は timeup/exhausted/complete/giveup |

---

## 6. 現在の課題（最優先）

### 認識精度による誤選択

**症状**: 書いた漢字とは別の字が認識候補に含まれ、それが条件を満たすため
選択肢に出てしまう。プレイヤーが誤って選べてしまう。

**具体例**
- 「研」を書いたのに「砕」が候補に出る（どちらも石を含む）
- 「糸＋布」という存在しない字を書いたら「終」と認識された

**原因**
`KanjiCanvas.recognize()` は常に10候補程度を返す。
現在の実装は**全候補を対象に絞り込んでいる**ため、下位候補で偶然一致する。

**測定結果**（初期テスト23回）
```
全候補の平均      10.0 個
該当候補の平均     0.30 個
該当0個: 18回 / 1個: 4回 / 2個以上: 1回
```
該当が複数出るケース自体は稀。ただし出たときに誤選択が起きる。

### 対策案（実装優先度順）

**A. 候補数を絞る（最も簡単・効果大）**

`judge()` 内で `cands` を上位3つに制限する。1行で済む。

```javascript
const cands = String(KanjiCanvas.recognize('can') || '')
  .trim().split(/\s+/).filter(Boolean).slice(0, 3);
```

副作用: 正しく書いたのに候補から漏れる可能性が上がる。
**上位3・5・10で実際に試して、体感の良い値を決めること。**

**B. 画数でフィルタする**

KanjiCanvas は書かれたストローク数を保持している。
`ref-patterns.js` の各漢字のパターンデータからも画数が取得できるはず。
**書いた画数と一致しない候補を除外する**だけで精度が大きく上がる。

要調査:
- KanjiCanvas がストローク配列をどこに保持しているか
- `ref-patterns.js` のデータ構造から画数を取れるか

**C. KanjiCanvas 自体を改造する**

MIT ライセンスなので改変可能。認識スコアの閾値を設けて、
一定以下の候補を捨てる、など。

要調査: `recognize()` がスコアを内部で持っているか、外部から取れるか。

### 調べる価値のないこと

以下は検証済みで、いずれも使えない。**再検討に時間を使わないこと。**

| 手段 | 結果 |
|---|---|
| Chrome Handwriting Recognition API | 日本語が環境非対応 |
| Gemini API（画像で認識） | 平均3.5秒。精度も不十分 |
| Google Input Tools 手書きAPI | 正式公開APIではなく無断利用になる |
| モジナビの内部API | 同上 |
| ユーザーローカル手書き認識API | 資料請求フォーム経由で日数が読めない |
| tegaki-kaki 方式 | 精度が不足 |

---

## 7. 残作業

| 優先 | 内容 |
|---|---|
| 1 | **認識精度の改善**（6章） |
| 2 | 他者にプレイしてもらう。「もう一度やりたいか」を確認する |
| 3 | **見た目・演出・効果音**（審査で最も効く。4日は確保したい） |
| 4 | 対戦モード（同一端末で交互に操作。受け渡し画面を挟む） |
| 5 | タイトル・遊び方説明・クレジット表示 |
| 6 | 応募資料、紹介動画 |

### 対戦モードの設計（未実装）

サーバー不要。1台の端末を2人で受け渡す。

```
[攻撃] 手番プレイヤーに5つの部首を提示 → 1つ選ぶ
  ↓
[受け渡し] 「相手に端末を渡してください」
  ↓
[防御] 30秒カウントダウン → 手書きで答える
  ↓
  ├ 成功 → 攻守交代
  └ 時間切れ → 結果画面（未使用漢字の提示）
```

受け渡し画面が必要な理由: 攻撃側が選んだ部首を防御側が事前に見ると、
考える時間が生まれてゲームが成立しない。

---

## 8. コーディング方針

- **単一ファイルを維持する。**分割すると審査員が動かすときの手間が増える
- 外部ライブラリを追加しない（KanjiCanvas 以外）
- localStorage は使わない（現状メモリ内のみ）
- 定数は先頭にまとめる（`TIME_LIMIT`, `MAX_REVEAL`, `GRADES`, `GLABEL`）
- 日本語のUIテキストは、小学生が読める語を使う

---

## 9. 未確定事項

- [ ] 認識候補の上限（3 / 5 / 10 のどれか）
- [ ] 制限時間30秒の妥当性
- [ ] 上級のコンプリートモード（氵115字は箱が多すぎる可能性）
- [ ] 効果音の有無と素材
- [ ] 「人」に混ざっている大・火・金・茶・内を除外するか
