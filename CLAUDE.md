# 開発ルール

## コミット

- 意味のある変更をしたら、そのつどわかりやすいコミットメッセージでコミットする
  （何を・なぜ変更したかが伝わる日本語で。既存のコミットログの書き方に合わせる）
- 1つのコミットは1つの関心事にまとめる。無関係な変更を1つのコミットに混ぜない
- コミットしてよいかは毎回ユーザーに確認を取らなくてよい（ユーザー側で後から確認する運用）
- ただし以下は別扱いで、必ずユーザーの明示的な許可を取ってから行う。
  勝手に実行しない（基本的にやってはいけないこととする）:
  - リモートへのpush（特にmain/masterブランチへのpush）
  - force push、`git reset --hard`、`git checkout --`／`git clean -f`などの
    破壊的操作（作業中の変更を消す可能性がある操作）
  - 既存コミットの`--amend`（新しいコミットを作るのが基本）
  - pre-commitフックのスキップ（`--no-verify`）やGPG署名の無効化

## その他

- フロントエンドはビルド不要のバニラJSを維持する（`docs/GAME_DESIGN.md`参照）
- ゲーム設計・データ仕様・既知の課題は`docs/GAME_DESIGN.md`・`docs/BACKEND.md`・
  `docs/DEVLOG.md`にまとまっているので、作業前に確認する
